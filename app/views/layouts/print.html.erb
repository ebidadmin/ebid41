<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>
      <%= content_for?(:title) ? "Print | " + yield(:title) : "E-Bid Inc. - The Philippines' Vehicle Parts Auction Hub" %>
    </title>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
			<%= javascript_include_tag 'html5shiv.js' %>
    <![endif]-->

		<%= stylesheet_link_tag 'print', media: 'all' %>
    <%= yield(:head) %>
    <%= csrf_meta_tags %>
    <link href="/assets/favicon.ico" rel="shortcut icon">
  </head>

  <body class="<%= params[:controller] %>">
 		<div class="container">
			<div class="well well-small btn-toolbar">
				<%= yield(:nav) %>
				<div class="btn-group pull-right">
		     	<%= link_with_icon('Back', :back, 'backward') %>
		    	<%= link_with_icon('Home', relative_root_path, 'home') %>
					<%= link_with_icon('Print', '#', 'print', nil, false, true) %>
				</div>
				<%= clearfix_div %>
			</div>
			<%= render 'layouts/alert_messages' %>
			<%= content_tag :h1 do %>
				<%= yield(:title) %>
				<%= yield(:subtitle).html_safe %>
			<% end %>
      <%= yield %>
			<hr>
			<%= content_tag :p, class: 'small' do %>
				<%= content_tag :strong, "Copyright &copy; 2006-#{Time.now.year} E-Bid, Inc. All rights reserved.".html_safe %>
				All rights reserved.
			<% end %>
    </div>
		<div id="javascripts">
	    <%= javascript_include_tag "application" %>
		</div>
  </body>
</html>
