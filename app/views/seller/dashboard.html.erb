<% title 'Seller Dashboard' %>
<% subtitle current_user.fullname %>

<div class="row txtcenter">
	<div class="span5 well">
		<%= content_tag :h2, 'Entries' %>
		<div class="row">
			<div class="span2">
				<%= content_tag :h4, 'Online' %>
				<%= content_tag :p, @presenter.online_entries.count, class: 'lead' %>
				<%= link_to "Let's Bid!", seller_entries_path(s: 'online'), class: 'btn btn-primary' %>
			</div>
			<div class="span2 offset1">
				<%= content_tag :h4, 'For Decision' %>
				<%= content_tag :p, @presenter.for_decision_entries.count, class: 'lead' %>
				<%= link_to 'View Entries', seller_entries_path(s: 'for-decision'), class: 'btn' %>
			</div>
			<div class="span5 alert-info bottom-bar">
				<%= content_tag :b, 'Bidding Ratio' %>
				<%= content_tag :span, @presenter.bidding_ratio, class: 'lead' %>
				<%= content_tag :p, @presenter.bidding_ratio_details, class: 'small' %>
			</div>
			<% if @presenter.bidding_ratio < "25%" %>
			<div class="span5 alert-success bottom-bar">
				<%= content_tag :strong, 'Tip', class: 'label label-success' %>
				<%= content_tag :span, "Target a bidding ratio of at least 25% to get a steady number of orders. So keep on bidding!" %>
				<%= content_tag :i, '', class: 'icon-arrow-right' %>
			</div>
			<% end %>
		</div>
	</div>
	<div class="span6 well">
		<%= content_tag :h2, 'Orders' %>
		<div class="row">
			<div class="span2">
				<%= content_tag :h4, 'New' %>
				<%= content_tag :p, @presenter.new_orders.count, class: 'lead' %>
				<% unless @presenter.new_orders.blank? %>
					<%= content_tag :p, (currency @presenter.new_orders.sum(:order_total), 'P '), class: 'label' %>
					<%= link_to 'View POs', seller_orders_path(s: 'new'), class: 'btn' %>
				<% end %>
			</div>
			<div class="span2">
				<%= content_tag :h4, 'Delivered' %>
				<%= content_tag :p, @presenter.delivered.count, class: 'lead' %>
				<% unless @presenter.delivered.blank? %>
					<%= content_tag :p, (currency @presenter.delivered.sum(:order_total), 'P '), class: 'label label-warning' %>
					<%= content_tag :h5, 'Sent Invoices?' %>
					<%= link_to 'View POs', seller_orders_path(s: 'delivered'), class: 'btn btn-warning' %>
				<% end %>	
			</div>
			<div class="span2<%= ' alert-error' unless @presenter.overdue.blank? %>">
				<%= content_tag :h4, 'Overdue' %>
				<%= content_tag :p, @presenter.overdue.count, class: 'lead' %>
				<% unless @presenter.overdue.blank? %>
					<%= content_tag :p, (currency @presenter.overdue.sum(:order_total), 'P '), class: 'label label-important overdue' %>
					<%= content_tag :h5, 'We follow-up!' %>
					<%= content_tag :p, (link_to 'View POs', seller_orders_path(s: 'overdue'), class: 'btn btn-danger') %>
				<% end %>
			</div>
			<div class="span6 alert-info bottom-bar">
				<%= content_tag :b, 'Total orders you got from E-Bid: ' %>
				<%= content_tag :span, (currency @presenter.total_orders, 'P'), class: 'lead' %>
			</div>
		</div>
	</div>
</div>

<div class="well">
	<%= content_tag :h2, 'Latest Messages' %>
	<%= render 'messages/table_index' %>
	<%= content_tag :p, (link_to 'Other Messages', seller_messages_path, class: 'btn'), class: 'txtright' %>
</div>

