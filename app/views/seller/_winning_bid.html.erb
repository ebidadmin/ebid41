<% if !item.is_online %>
	<% lo_bid = bids.not_cancelled.last %>
	<% if lo_bid != own_bid  %>
		<%= content_tag :code, class: "small" do %>
			<%= content_tag :span, currency(lo_bid.amount) %>
			<%= content_tag :em, 'winner' %>
		<% end %>
	<% end %>
<% end %>
